@using Core;
@model List<Food>
<h2>Food</h2>


<table style="width:60%">
    <tr>
        <th>Company</th>
        <th>Contact</th>
    </tr>
    @foreach (var f in Model)
    {
        <tr>
            <td>@f.FoodID</td>
            <td>@f.FoodName</td>
        </tr>
    }
</table>
